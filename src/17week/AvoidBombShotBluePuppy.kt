package `17week`

fun main() {
    val (N, M, T) = readln().split(" ").map { it.toInt() }
    val group = readln().split(" ").map { it.toInt() }
    val P = readln().toInt()

    // 시간에 따라 이동할 수 있는 범위의 최댓값이 M이 되는지 체크하는 로직
}
